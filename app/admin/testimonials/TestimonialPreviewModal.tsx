"use client";

import { Badge } from "@/components/ui/badge";
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { MessageSquare, Quote, Star, User } from "@/lib/icons";
import type { AdminTestimonial } from "./TestimonialTable";

function formatDate(value: string | null) {
  if (!value) return "-";
  return new Intl.DateTimeFormat("id-ID", { dateStyle: "medium", timeStyle: "short" }).format(new Date(value));
}

function getInitials(name: string) {
  return name.split(" ").map((part) => part[0]).filter(Boolean).join("").slice(0, 2).toUpperCase() || "IR";
}

export function TestimonialPreviewModal({ testimonial, open, onOpenChange }: { testimonial: AdminTestimonial | null; open: boolean; onOpenChange: (open: boolean) => void }) {
  if (!testimonial) return null;
  const identity = [testimonial.jabatan, testimonial.perusahaan].filter(Boolean).join(" · ") || "No role/company metadata";

  return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="max-h-[90vh] max-w-5xl overflow-y-auto rounded-none border-primary/20 bg-background/95 p-0 shadow-2xl shadow-primary/10 backdrop-blur-xl"><DialogHeader className="relative overflow-hidden border-b border-border/50 bg-gradient-to-br from-primary/15 via-card to-cyan-500/10 p-6"><div className="absolute -right-10 -top-10 h-44 w-44 rounded-full bg-primary/20 blur-3xl" /><div className="relative flex flex-col gap-5 md:flex-row md:items-start md:justify-between"><div className="flex items-start gap-4"><div className="flex h-12 w-12 items-center justify-center border border-primary/30 bg-primary/10 text-primary"><MessageSquare className="h-6 w-6" /></div><div><p className="font-mono text-xs uppercase tracking-[0.35em] text-primary/70">CLIENT_PROOF_RECORD</p><DialogTitle className="mt-1 font-orbitron text-2xl text-primary">TESTIMONIAL_DETAIL</DialogTitle><DialogDescription className="mt-2 max-w-2xl text-sm text-muted-foreground">Review testimonial quality, public visibility, featured placement, client identity, and database metadata.</DialogDescription></div></div><div className="flex flex-wrap gap-2"><Badge variant={testimonial.tampil ? "default" : "secondary"} className="rounded-none font-mono">{testimonial.tampil ? "PUBLIC_VISIBLE" : "HIDDEN"}</Badge><Badge variant={testimonial.featured ? "default" : "secondary"} className="rounded-none font-mono">{testimonial.featured ? "FEATURED" : "STANDARD"}</Badge></div></div></DialogHeader><div className="grid gap-0 lg:grid-cols-[1fr_360px]"><main className="space-y-5 p-6"><section className="relative overflow-hidden border border-primary/20 bg-card/70 p-6 shadow-[0_0_45px_hsl(var(--primary)/0.08)]"><div className="absolute -left-8 -top-8 h-28 w-28 rounded-full bg-primary/10 blur-2xl" /><Quote className="relative mb-5 h-9 w-9 text-primary/70" /><p className="relative whitespace-pre-wrap text-base leading-8 text-foreground md:text-lg">{testimonial.isi}</p><div className="relative mt-6 flex flex-wrap items-center justify-between gap-4 border-t border-border/50 pt-5"><div className="flex gap-1">{[1, 2, 3, 4, 5].map((rating) => <Star key={rating} className={`h-5 w-5 ${testimonial.rating >= rating ? "fill-yellow-300 text-yellow-300" : "text-muted-foreground/35"}`} />)}</div><span className="border border-yellow-400/30 bg-yellow-400/10 px-3 py-1 font-mono text-xs text-yellow-300">QUALITY_SCORE {testimonial.rating}/5</span></div></section><section className="grid gap-3 md:grid-cols-3"><div className="border border-border/60 bg-secondary/30 p-4"><p className="font-mono text-[11px] uppercase tracking-[0.25em] text-muted-foreground">Visibility</p><p className="mt-2 font-orbitron text-lg text-foreground">{testimonial.tampil ? "Public" : "Hidden"}</p></div><div className="border border-border/60 bg-secondary/30 p-4"><p className="font-mono text-[11px] uppercase tracking-[0.25em] text-muted-foreground">Placement</p><p className="mt-2 font-orbitron text-lg text-foreground">{testimonial.featured ? "Featured" : "Standard"}</p></div><div className="border border-border/60 bg-secondary/30 p-4"><p className="font-mono text-[11px] uppercase tracking-[0.25em] text-muted-foreground">Characters</p><p className="mt-2 font-orbitron text-lg text-foreground">{testimonial.isi.length}</p></div></section><section className="grid gap-3 font-mono text-xs text-muted-foreground md:grid-cols-2"><div className="break-all border border-border/50 bg-background/60 p-3"><span className="text-primary">ID:</span> {testimonial.id}</div><div className="border border-border/50 bg-background/60 p-3"><span className="text-primary">Created:</span> {formatDate(testimonial.created_at)}</div><div className="break-all border border-border/50 bg-background/60 p-3 md:col-span-2"><span className="text-primary">Avatar:</span> {testimonial.avatar_url || "-"}</div></section></main><aside className="border-t border-border/50 bg-card/50 p-6 lg:border-l lg:border-t-0"><div className="sticky top-4 space-y-5"><div className="relative overflow-hidden border border-primary/20 bg-background/80 p-5 text-center"><div className="absolute inset-x-8 top-0 h-px bg-gradient-to-r from-transparent via-primary to-transparent" /><div className="mx-auto flex h-24 w-24 items-center justify-center overflow-hidden border border-primary/30 bg-secondary text-primary shadow-[0_0_35px_hsl(var(--primary)/0.12)]">{testimonial.avatar_url ? <img src={testimonial.avatar_url} alt={`${testimonial.nama} avatar`} className="h-full w-full object-cover" /> : <User className="h-10 w-10" />}</div><h3 className="mt-4 font-orbitron text-xl text-foreground">{testimonial.nama || getInitials(testimonial.nama)}</h3><p className="mt-1 text-sm text-muted-foreground">{identity}</p></div><div className="space-y-2"><p className="font-mono text-xs uppercase tracking-[0.3em] text-primary/70">STATUS_MATRIX</p><div className="grid grid-cols-2 gap-2 font-mono text-[11px]"><span className={`border px-2 py-2 text-center ${testimonial.tampil ? "border-emerald-400/30 bg-emerald-400/10 text-emerald-300" : "border-border/60 text-muted-foreground"}`}>{testimonial.tampil ? "PUBLIC" : "HIDDEN"}</span><span className={`border px-2 py-2 text-center ${testimonial.featured ? "border-primary/40 bg-primary/10 text-primary" : "border-border/60 text-muted-foreground"}`}>{testimonial.featured ? "FEATURED" : "STANDARD"}</span></div></div><div className="border border-border/50 bg-secondary/20 p-4"><p className="font-mono text-xs uppercase tracking-[0.25em] text-muted-foreground">Client Identity</p><dl className="mt-4 space-y-3 text-sm"><div><dt className="font-mono text-[11px] text-primary">NAME</dt><dd className="break-words text-foreground">{testimonial.nama}</dd></div><div><dt className="font-mono text-[11px] text-primary">ROLE</dt><dd className="break-words text-muted-foreground">{testimonial.jabatan || "-"}</dd></div><div><dt className="font-mono text-[11px] text-primary">COMPANY</dt><dd className="break-words text-muted-foreground">{testimonial.perusahaan || "-"}</dd></div></dl></div></div></aside></div></DialogContent></Dialog>;
}
